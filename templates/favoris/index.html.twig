{% extends 'base.html.twig' %}

{% block title %}Vos favoris - FlockYou{% endblock %}

{% block body %}

<div class="container">
    <h2 class="deepshadow">Vos favoris</h2>
    <div class="login-form bg-light py-3 px-3">
        {# {% if products | length > 0 %}
        {% for product in products %} #}
        <div class="fav row">
            <div class="image col-2">
                <a href="#">
                </a>
            </div>
            <div class="info col-8">
                <h4></h4>
                <h5 class="price mt-2"> €</h5>

                <span class="stock mt-2">
                    <i class="fas fa-exclamation-circle mr-2"></i>Seulement restants
                </span>

                <span class="stock mt-2">
                    <i class="fas fa-exclamation-triangle mr-2"></i>Seulement restant
                </span>

                <span class="stock mt-2">
                    <i class="fas fa-exclamation-triangle mr-2"></i>Produit indisponible
                </span>

            </div>
            <div class="action col-2">
                <a href="#" style="color: red"><i class="suppr far fa-times-circle pull-right"></i></a>
            </div>
        </div>
        <hr>
        <div class="alert alert-danger">
            Vous n'avez aucun favoris...
        </div>

        </ul>
    </div>
</div>
{% endblock %}